Os repositórios spi_main e spi_init em /pem/axi/rtl/spitestes possuem 
os arquivos de simulação. O write_test foi um teste que não foi terminado.

Dentro de /spi_fpga possui os repositórios spi_init, spi_main_simple e 
spi_main que possuem todos os arquivos para FPGA para inicialização, 
comunicação spi simples e comunicação spi quad.